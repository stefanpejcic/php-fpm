services:
  php-fpm-8.4:
    build:
      context: ./8.4
      dockerfile: Dockerfile
    image: openpanel/php:8.4-fpm
    container_name: php-fpm-8.4
    restart: unless-stopped
    volumes:
      - html_data:/var/www/html/
      - ./sockets/php:/var/run/php # /var/run/php/php-fpm-8.4.sock
      - ./php.ini/8.4.ini:/usr/local/etc/php/php.ini
      - /etc/openpanel/wordpress/wp-cli.phar:/usr/local/bin/wp:ro
      - /etc/openpanel/php/composer/v2_composer.phar:/usr/local/bin/composer:ro
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    deploy:
      resources:
        limits:
          cpus: "${PHP_FPM_8_4_CPU:-0.125}"
          memory: "${PHP_FPM_8_4_RAM:-0.25G}"
          pids: 100
    networks:
      - www
      - db
